const validateFilePath = require('./validateFilePath');
const validateChangeLine = require('./validateChangeLine');
const validateCurrentFile = require('./validateCurrentFile');

module.exports = {
    validateFilePath,
    validateChangeLine,
    validateCurrentFile
};
